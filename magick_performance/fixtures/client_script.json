[
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Settings",
  "enabled": 1,
  "modified": "2026-06-11 09:59:29.597060",
  "module": "Magick Performance",
  "name": "Performance Settings Form",
  "script": "frappe.ui.form.on('Performance Settings', {\r\n\r\n    onload: function(frm) {\r\n        // Auto-fill rating criteria saat dokumen baru, jalankan hanya 1 kali\r\n        if (frm.is_new() && !frm.__criteria_auto_filled) {\r\n            frm.clear_table('rating_criteria');\r\n\r\n            const default_criteria = [\r\n                { criteria_name: 'Completes tasks within the predetermined timeframe', weightage_: 20 },\r\n                { criteria_name: 'Communicates critical information clearly with team', weightage_: 20 },\r\n                { criteria_name: 'Proactive rather than reactive during projects',      weightage_: 20 },\r\n                { criteria_name: 'Competencies / Behaviors',                           weightage_: 20 },\r\n                { criteria_name: 'Teamwork & Collaboration',                           weightage_: 20 },\r\n            ];\r\n\r\n            default_criteria.forEach(item => {\r\n                let row = frm.add_child('rating_criteria');\r\n                row.criteria_name = item.criteria_name;\r\n                frappe.model.set_value(row.doctype, row.name, 'weightage_', 20);\r\n            });\r\n\r\n            frm.__criteria_auto_filled = true;\r\n            frm.refresh_field('rating_criteria');\r\n        }\r\n    },\r\n\r\n    refresh: function(frm) {\r\n        // Blast Email button\r\n        if (!frm.is_new()) {\r\n            frm.add_custom_button(__('Blast Email'), function() {\r\n                frappe.confirm('Send email to all appraisees?', () => {\r\n                    frappe.call({\r\n                        method: \"blast_email_performance\",\r\n                        args: {\r\n                            docname: frm.doc.name\r\n                        },\r\n                        callback: function(r) {\r\n                            // response ditangani oleh server script via frappe.msgprint\r\n                        }\r\n                    });\r\n                });\r\n            }, __(\"Actions\"));\r\n        }\r\n    },\r\n\r\n    get_employee: function(frm) {\r\n        if (!frm.doc.company) {\r\n            frappe.msgprint(__('Pilih Company terlebih dahulu.'));\r\n            return;\r\n        }\r\n        frappe.call({\r\n            method: 'frappe.client.get_list',\r\n            args: {\r\n                doctype: 'Employee',\r\n                filters: [\r\n                    ['company', '=', frm.doc.company],\r\n                    ['status',  '=', 'Active']\r\n                ],\r\n                limit_page_length: 1000,\r\n                fields: ['name', 'employee_name', 'department', 'designation', 'branch']\r\n            },\r\n            callback: function(r) {\r\n                if (r.message && r.message.length > 0) {\r\n                    frm.clear_table('appraisees');\r\n                    r.message.forEach(emp => {\r\n                        let row = frm.add_child('appraisees');\r\n                        row.employee      = emp.name;\r\n                        row.employee_name = emp.employee_name;\r\n                        row.department    = emp.department;\r\n                        row.designation   = emp.designation;\r\n                        row.branch        = emp.branch;\r\n                    });\r\n                    frm.refresh_field('appraisees');\r\n                    frappe.msgprint(__('Success.'));\r\n                } else {\r\n                    frappe.msgprint(__('There is no active employee in this company.'));\r\n                }\r\n            }\r\n        });\r\n    }\r\n});",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Evaluation",
  "enabled": 1,
  "modified": "2026-06-11 14:21:47.266277",
  "module": "Magick Performance",
  "name": "Performance Evaluation Self rating",
  "script": "frappe.ui.form.on('Performance Evaluation', {\r\n    refresh: function(frm) {\r\n        manage_rating_access(frm);\r\n        lock_weightage(frm);\r\n    },\r\n    onload: function(frm) {\r\n        manage_rating_access(frm);\r\n        lock_weightage(frm);\r\n    }\r\n});\r\n\r\n// Trigger untuk Tabel Goal Progression — kunci weightage_ saat baris dibuka\r\nfrappe.ui.form.on('Goal Progression Detail', {\r\n    form_render: function(frm, cdt, cdn) {\r\n        lock_weightage(frm);\r\n    }\r\n});\r\n\r\n// Trigger untuk Tabel Self Review (Karyawan)\r\nfrappe.ui.form.on('Self Review Detail', {\r\n    form_render: function(frm, cdt, cdn) {\r\n        manage_rating_access(frm);\r\n    }\r\n});\r\n\r\n// Trigger untuk Tabel Manager Review (Line Manager)\r\nfrappe.ui.form.on('Manager Review Detail', {\r\n    form_render: function(frm, cdt, cdn) {\r\n        manage_rating_access(frm);\r\n    }\r\n});\r\n\r\n\r\n// ----------------------------------------------------------------\r\n// Kunci weightage_ di child table Goal Progression\r\n// Berlaku untuk SEMUA user tanpa pengecualian\r\n// ----------------------------------------------------------------\r\nfunction lock_weightage(frm) {\r\n    // Kunci di level grid (mencegah edit langsung di tabel)\r\n    frm.fields_dict['goal_progression'].grid.update_docfield_property(\r\n        'weightage_', 'read_only', 1\r\n    );\r\n\r\n    // Kunci di level dialog (mencegah edit saat baris dibuka via ikon pensil)\r\n    setTimeout(() => {\r\n        $('[data-fieldname=\"weightage_\"] input').prop('disabled', true);\r\n        $('[data-fieldname=\"weightage_\"] input').css({\r\n            'pointer-events': 'none',\r\n            'background'    : 'var(--disabled-control-bg)',\r\n            'color'         : 'var(--disabled-text-color)',\r\n        });\r\n    }, 300);\r\n}\r\n\r\n\r\n// ----------------------------------------------------------------\r\n// Kelola akses rating berdasarkan role\r\n// ----------------------------------------------------------------\r\nfunction manage_rating_access(frm) {\r\n\r\n    // -------------------------------------------------------------\r\n    // KONDISI 1: EMPLOYEE (bukan Line Manager)\r\n    // -------------------------------------------------------------\r\n    if (frappe.user.has_role('Employee') && !frappe.user.has_role('Line Manager')) {\r\n\r\n        frm.set_df_property('manager_review', 'read_only', 1);\r\n\r\n        setTimeout(() => {\r\n            $('[data-fieldname=\"manager_rating\"] .rating').css('pointer-events', 'none');\r\n            $('.grid-row [data-fieldname=\"manager_rating\"]').css('pointer-events', 'none');\r\n        }, 300);\r\n\r\n        if (frm.doc.status !== 'Draft') {\r\n            frm.set_df_property('self_review', 'read_only', 1);\r\n            setTimeout(() => {\r\n                $('[data-fieldname=\"self_rating\"] .rating').css('pointer-events', 'none');\r\n                $('.grid-row [data-fieldname=\"self_rating\"]').css('pointer-events', 'none');\r\n            }, 300);\r\n        }\r\n    }\r\n\r\n    // -------------------------------------------------------------\r\n    // KONDISI 2: LINE MANAGER\r\n    // -------------------------------------------------------------\r\n    if (frappe.user.has_role('Line Manager')) {\r\n\r\n        frm.set_df_property('self_review', 'read_only', 1);\r\n\r\n        setTimeout(() => {\r\n            $('[data-fieldname=\"self_rating\"] .rating').css('pointer-events', 'none');\r\n            $('.grid-row [data-fieldname=\"self_rating\"]').css('pointer-events', 'none');\r\n        }, 300);\r\n\r\n        if (frm.doc.docstatus < 2) {\r\n            frm.set_df_property('manager_review', 'read_only', 0);\r\n            setTimeout(() => {\r\n                $('[data-fieldname=\"manager_rating\"] .rating').css('pointer-events', 'auto');\r\n                $('.grid-row [data-fieldname=\"manager_rating\"]').css('pointer-events', 'auto');\r\n            }, 300);\r\n        }\r\n    }\r\n}",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Evaluation",
  "enabled": 1,
  "modified": "2026-06-05 12:25:09.738414",
  "module": "Magick Performance",
  "name": "Performance Evaluation Description",
  "script": "frappe.ui.form.on('Performance Evaluation', {\r\n    refresh: function(frm) {\r\n        // Hapus elemen deskripsi lama jika ada agar tidak duplikat saat refresh\r\n        frm.fields_dict['goal_progression'].grid.wrapper.find('.custom-grid-desc').remove();\r\n        \r\n        // Tambahkan deskripsi baru yang menggunakan variabel CSS bawaan Frappe\r\n        frm.fields_dict['goal_progression'].grid.wrapper.find('.grid-body').after(`\r\n            <div class=\"p-3 custom-grid-desc\" style=\"\r\n                font-size: 12px; \r\n                background-color: var(--bg-light-gray); \r\n                color: var(--text-muted);\r\n                border: 1px solid var(--border-color); \r\n                border-top: none; \r\n                border-radius: 0 0 8px 8px;\r\n            \">\r\n                <div style=\"margin-bottom: 12px;\">\r\n                    <p class=\"mb-0\" style=\"color: var(--text-muted);\">\r\n                        <strong style=\"color: var(--text-color);\">Employee Progression (%):</strong> To be completed by the Employee. Please state your estimated goal completion progression by the end of the year based on your actual achievements.\r\n                    </p>\r\n                </div>\r\n                <hr style=\"border-top: 1px solid var(--border-color); margin: 10px 0;\">\r\n                <div>\r\n                    <p class=\"mb-0\" style=\"color: var(--text-muted);\">\r\n                        <strong style=\"color: var(--text-color);\">Manager Adjusted Progression (%):</strong> To be completed by the Line Manager. Please review and input the final calibrated progress percentage based on your objective performance evaluation.\r\n                    </p>\r\n                </div>\r\n            </div>\r\n        `);\r\n    }\r\n});",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Evaluation",
  "enabled": 1,
  "modified": "2026-09-17 19:01:17.263602",
  "module": "Magick Performance",
  "name": "Performance Evaluation",
  "script": "frappe.ui.form.on('Performance Evaluation', {\n    refresh: function(frm) {\n        // Scores are authoritative from the server (Before Save script); only render the band here.\n        render_performance_category(frm, frm.doc.final_score || 0);\n    }\n});\n\nfrappe.ui.form.on('Goal Progression Detail', {\n    employee_progression_: function(frm) { calculate_goal_score(frm); },\n    manager_adjusted_progression_: function(frm) { calculate_goal_score(frm); },\n    weightage_: function(frm) { calculate_goal_score(frm); },\n    goal_progression_remove: function(frm) { calculate_goal_score(frm); }\n});\n\nfrappe.ui.form.on('Self Review Detail', {\n    self_rating: function(frm) { calculate_self_score(frm); },\n    weightage_: function(frm) { calculate_self_score(frm); },\n    self_review_remove: function(frm) { calculate_self_score(frm); }\n});\n\nfrappe.ui.form.on('Manager Review Detail', {\n    manager_rating: function(frm) { calculate_manager_score(frm); },\n    weightage_: function(frm) { calculate_manager_score(frm); },\n    manager_review_remove: function(frm) { calculate_manager_score(frm); }\n});\n\nasync function calculate_goal_score(frm) {\n    let total = 0;\n    let table_field = frm.doc.goal_progression_ ? 'goal_progression_' : 'goal_progression';\n    let rows = frm.doc[table_field] || [];\n    let kra_setup = null;\n    if (frm.doc.kra_and_goal_setup) {\n        kra_setup = await frappe.db.get_doc('KRA and Goal Setup', frm.doc.kra_and_goal_setup);\n    }\n    for (let row of rows) {\n        let p_emp = flt(row.employee_progression_);\n        let p_man = flt(row.manager_adjusted_progression_);\n        let weight = flt(row.weightage_);\n        if (weight === 0 && kra_setup) {\n            let detail = kra_setup.kra_and_goal_detail.find(d => d.kra_name === row.kra_name);\n            if (detail) weight = flt(detail.weightage_);\n        }\n        let actual_prog = p_man > 0 ? p_man : p_emp; // manager-adjusted wins once set, else the employee's own progression (MP-Q6)\n        total += (actual_prog / 100) * (weight / 100) * 5;\n    }\n    frm.set_value('total_goal_score', flt(total, 2)); // never flt() a string: locale id reads '4.00' as 400 (MP-Q4)\n    calculate_final_score(frm);\n}\n\nfunction calculate_self_score(frm) {\n    let rows = frm.doc.self_review_ || frm.doc.self_review || [];\n    let total_poin = 0;\n    rows.forEach(row => {\n        let rating = flt(row.self_rating) * 5; // star rating ERPNext: 0–1, kalikan 5\n        let weight = flt(row.weightage_);\n        if (weight > 0) {\n            total_poin += flt((rating * weight) / 100);\n        }\n    });\n    frm.set_value('total_self_score', flt(total_poin, 2));\n    calculate_final_score(frm);\n}\n\nfunction calculate_manager_score(frm) {\n    let rows = frm.doc.manager_review_ || frm.doc.manager_review || [];\n    let total_poin = 0;\n    rows.forEach(row => {\n        let rating = flt(row.manager_rating) * 5; // star rating ERPNext: 0–1, kalikan 5\n        let weight = flt(row.weightage_);\n        if (weight > 0) {\n            total_poin += flt((rating * weight) / 100);\n        }\n    });\n    frm.set_value('total_manager_score', flt(total_poin, 2));\n    calculate_final_score(frm);\n}\n\nfunction calculate_final_score(frm) {\n    let g = flt(frm.doc.total_goal_score);\n    let s = flt(frm.doc.total_self_score);\n    let m = flt(frm.doc.total_manager_score);\n    let final = flt((g + s + m) / 3, 2);\n    frm.set_value('final_score', final);\n    render_performance_category(frm, final);\n}\n\nvar PERF_CATEGORIES = [\n    { score: 5, label: \"Outstanding\",          range: \"95 – 100%\", min: 4.75, max: 5,    color: \"#16a34a\", bg: \"#f0fdf4\" },\n    { score: 4, label: \"Exceeds Expectations\", range: \"85 – 94%\",  min: 4.0,  max: 4.74, color: \"#65a30d\", bg: \"#f7fee7\" },\n    { score: 3, label: \"Meets Expectations\",   range: \"70 – 84%\",  min: 3.0,  max: 3.99, color: \"#2563eb\", bg: \"#eff6ff\" },\n    { score: 2, label: \"Fair\",                 range: \"55 – 69%\",  min: 2.0,  max: 2.99, color: \"#ea580c\", bg: \"#fff7ed\" },\n    { score: 1, label: \"Needs Improvement\",    range: \"Below 55%\", min: 0,    max: 1.99, color: \"#dc2626\", bg: \"#fef2f2\" }\n];\n\nfunction render_performance_category(frm, final_score) {\n    if (!frm.fields_dict[\"performance_category_html\"]) return;\n    var val = flt(final_score);\n    var active = PERF_CATEGORIES.find(c => val >= c.min && val <= c.max) || PERF_CATEGORIES[4];\n    var rows = PERF_CATEGORIES.map(cat => {\n        var is_active = cat.score === active.score;\n        return `<tr style=\"background:${is_active ? cat.bg : \"transparent\"}\">\n            <td style=\"padding:8px 10px;text-align:center;font-weight:600;color:${cat.color};border:1px solid #e5e7eb\">${cat.score}</td>\n            <td style=\"padding:8px 10px;color:${cat.color};border:1px solid #e5e7eb\">${cat.label}</td>\n            <td style=\"padding:8px 10px;color:${cat.color};border:1px solid #e5e7eb\">${cat.range}</td>\n        </tr>`;\n    }).join(\"\");\n    frm.fields_dict[\"performance_category_html\"].$wrapper.html(`\n        <div style=\"margin-top:8px;margin-bottom:20px;\">\n            <table style=\"width:50%;border-collapse:collapse;font-size:13px;border:1px solid #e5e7eb;\">\n                <tr style=\"background:#1e3a5f;color:#fff\">\n                    <th style=\"padding:8px 10px;text-align:center;width:80px;\">Score</th>\n                    <th style=\"padding:8px 10px;text-align:left;\">Label</th>\n                    <th style=\"padding:8px 10px;text-align:left;\">Range</th>\n                </tr>${rows}\n            </table>\n        </div>`);\n}",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Settings",
  "enabled": 1,
  "modified": "2026-06-11 09:59:29.597060",
  "module": "Magick Performance",
  "name": "Perfomance Settings",
  "script": "frappe.ui.form.on('Performance Settings', {\r\n\r\n    onload: function(frm) {\r\n        // Auto-fill rating criteria saat dokumen baru, jalankan hanya 1 kali\r\n        if (frm.is_new() && !frm.__criteria_auto_filled) {\r\n            frm.clear_table('rating_criteria');\r\n\r\n            const default_criteria = [\r\n                { criteria_name: 'Completes tasks within the predetermined timeframe', weightage_: 20 },\r\n                { criteria_name: 'Communicates critical information clearly with team', weightage_: 20 },\r\n                { criteria_name: 'Proactive rather than reactive during projects',      weightage_: 20 },\r\n                { criteria_name: 'Competencies / Behaviors',                           weightage_: 20 },\r\n                { criteria_name: 'Teamwork & Collaboration',                           weightage_: 20 },\r\n            ];\r\n\r\n            default_criteria.forEach(item => {\r\n                let row = frm.add_child('rating_criteria');\r\n                row.criteria_name = item.criteria_name;\r\n                frappe.model.set_value(row.doctype, row.name, 'weightage_', 20);\r\n            });\r\n\r\n            frm.__criteria_auto_filled = true;\r\n            frm.refresh_field('rating_criteria');\r\n        }\r\n    },\r\n\r\n    refresh: function(frm) {\r\n        // Blast Email button\r\n        if (!frm.is_new()) {\r\n            frm.add_custom_button(__('Blast Email'), function() {\r\n                frappe.confirm('Send email to all appraisees?', () => {\r\n                    frappe.call({\r\n                        method: \"blast_email_performance\",\r\n                        args: {\r\n                            docname: frm.doc.name\r\n                        },\r\n                        callback: function(r) {\r\n                            // response ditangani oleh server script via frappe.msgprint\r\n                        }\r\n                    });\r\n                });\r\n            }, __(\"Actions\"));\r\n        }\r\n    },\r\n\r\n    get_employee: function(frm) {\r\n        if (!frm.doc.company) {\r\n            frappe.msgprint(__('Pilih Company terlebih dahulu.'));\r\n            return;\r\n        }\r\n        frappe.call({\r\n            method: 'frappe.client.get_list',\r\n            args: {\r\n                doctype: 'Employee',\r\n                filters: [\r\n                    ['company', '=', frm.doc.company],\r\n                    ['status',  '=', 'Active']\r\n                ],\r\n                limit_page_length: 1000,\r\n                fields: ['name', 'employee_name', 'department', 'designation', 'branch']\r\n            },\r\n            callback: function(r) {\r\n                if (r.message && r.message.length > 0) {\r\n                    frm.clear_table('appraisees');\r\n                    r.message.forEach(emp => {\r\n                        let row = frm.add_child('appraisees');\r\n                        row.employee      = emp.name;\r\n                        row.employee_name = emp.employee_name;\r\n                        row.department    = emp.department;\r\n                        row.designation   = emp.designation;\r\n                        row.branch        = emp.branch;\r\n                    });\r\n                    frm.refresh_field('appraisees');\r\n                    frappe.msgprint(__('Success.'));\r\n                } else {\r\n                    frappe.msgprint(__('There is no active employee in this company.'));\r\n                }\r\n            }\r\n        });\r\n    }\r\n});",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Evaluation",
  "enabled": 1,
  "modified": "2026-09-17 19:20:42.183706",
  "module": "Magick Performance",
  "name": "Performance evaluation auto-populate table",
  "script": "frappe.ui.form.on('Performance Evaluation', {\n    kra_and_goal_setup: function(frm) {\n        if (!frm.doc.kra_and_goal_setup) return;\n\n        frappe.db.get_doc('KRA and Goal Setup', frm.doc.kra_and_goal_setup)\n            .then(kra_setup => {\n                // MP-Q8: fetch_from fields (company, cycle_year) are still empty here; take them from the KRA doc.\n                let company = frm.doc.company || kra_setup.company;\n                let cycle_year = frm.doc.cycle_year || kra_setup.cycle_year;\n                frappe.db.get_value('Performance Settings',\n                    {company: company, cycle_year: cycle_year},\n                    ['base_year', 'rating_criteria'],\n                    function(r) {\n                        let base_year = r && r.base_year ? r.base_year : cycle_year;\n                        let yn = cycle_year - base_year + 1;\n\n                        // Auto-populate Goal Progression\n                        frm.clear_table('goal_progression');\n                        kra_setup.kra_and_goal_detail.forEach(row => {\n                            let child = frm.add_child('goal_progression');\n                            child.kra_name = row.kra_name;\n                            child.goal_name = row.kra_name + ' Y' + yn;\n                            child.goal_description = row.goal_description;\n                            child.weightage_ = row.weightage_;\n                        });\n                        frm.refresh_field('goal_progression');\n\n                        // Auto-populate Self Review & Manager Review\n                        // Perlu ambil full doc Performance Settings karena get_value tidak return child table\n                        frappe.db.get_doc('Performance Settings',\n                            `${company}-${cycle_year}`\n                        ).then(settings => {\n                            frm.clear_table('self_review');\n                            frm.clear_table('manager_review');\n\n                            settings.rating_criteria.forEach(criteria => {\n                                let self_row = frm.add_child('self_review');\n                                self_row.criteria_name = criteria.criteria_name;\n                                self_row.weightage_ = criteria.weightage_;\n\n                                let mgr_row = frm.add_child('manager_review');\n                                mgr_row.criteria_name = criteria.criteria_name;\n                                mgr_row.weightage_ = criteria.weightage_;\n                            });\n\n                            frm.refresh_field('self_review');\n                            frm.refresh_field('manager_review');\n\n                            // Hitung ulang score setelah populate\n                            calculate_goal_score(frm);\n                            calculate_self_score(frm);\n                            calculate_manager_score(frm);\n                        }).catch(() => {\n                            frappe.msgprint({\n                                title: 'Warning',\n                                message: 'Performance Settings not found for this company and cycle year.',\n                                indicator: 'orange'\n                            });\n                        });\n                    }\n                );\n            });\n    }\n});",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Settings",
  "enabled": 0,
  "modified": "2026-06-11 09:59:24.175259",
  "module": "Magick Performance",
  "name": "Rating Criteria Auto",
  "script": "frappe.ui.form.on('Performance Settings', {\r\n    onload: function(frm) {\r\n        // Flag agar hanya berjalan 1 kali saat dokumen baru dibuka\r\n        if (frm.is_new() && !frm.__criteria_auto_filled) {\r\n            \r\n            // 1. Bersihkan tabel\r\n            frm.clear_table('rating_criteria'); \r\n\r\n            // 2. Data 5 kriteria resmi\r\n            const default_criteria = [\r\n                { criteria_name: 'Completes tasks within the predetermined timeframe', weight: 20 },\r\n                { criteria_name: 'Communicates critical information clearly with team', weight: 20 },\r\n                { criteria_name: 'Proactive rather than reactive during projects', weight: 20 },\r\n                { criteria_name: 'Competencies / Behaviors', weight: 20 },\r\n                { criteria_name: 'Teamwork & Collaboration', weight: 20 }\r\n            ];\r\n\r\n            // 3. Masukkan data menggunakan frappe.model.set_value (Lebih Agresif)\r\n            default_criteria.forEach(item => {\r\n                let row = frm.add_child('rating_criteria');\r\n                \r\n                // Set nama kriteria\r\n                row.criteria_name = item.criteria_name; \r\n                \r\n                // Gunakan frappe.model.set_value untuk memaksa field 'weightage_' menerima angka 20 murni\r\n                frappe.model.set_value(row.doctype, row.name, 'weightage_', 20);\r\n            });\r\n\r\n            // Kunci flag\r\n            frm.__criteria_auto_filled = true;\r\n\r\n            // 4. Refresh field tabel agar perubahan ter-render di browser\r\n            frm.refresh_field('rating_criteria');\r\n        }\r\n    }\r\n});",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "KRA and Goal Setup",
  "enabled": 1,
  "modified": "2026-09-17 19:24:25.114440",
  "module": "Magick Performance",
  "name": "KRA and Goal Setup default employee",
  "script": "frappe.ui.form.on('KRA and Goal Setup', {\n    onload: function(frm) {\n        // MP-Q3: a new KRA defaults to the logged-in user's own Employee record.\n        if (!frm.is_new() || frm.doc.employee) return;\n        frappe.db.get_value('Employee', {user_id: frappe.session.user, status: 'Active'}, 'name').then(r => {\n            if (r && r.message && r.message.name) frm.set_value('employee', r.message.name);\n        });\n    }\n});",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "KRA and Goal Setup",
  "enabled": 1,
  "modified": "2026-09-17 19:24:25.117083",
  "module": "Magick Performance",
  "name": "KRA and Goal Setup employee link titles",
  "script": "// MP-Q2: ERPNext's global Employee link formatter labels every Employee link on a form with the form's\n// own employee_name, so \"Reports To\" showed the employee's name instead of the manager's. Stricter version.\nfrappe.form.link_formatters['Employee'] = function (value, doc, df) {\n    if (!doc || !value) return value;\n    if (doc.employee === value && doc.employee_name && doc.employee_name !== value) return value + ': ' + doc.employee_name;\n    if (df && df.fieldname === 'reports_to' && doc.line_manager_name) return value + ': ' + doc.line_manager_name;\n    return value;\n};\n",
  "view": "Form"
 },
 {
  "docstatus": 0,
  "doctype": "Client Script",
  "dt": "Performance Evaluation",
  "enabled": 1,
  "modified": "2026-09-17 19:24:25.117979",
  "module": "Magick Performance",
  "name": "Performance Evaluation employee link titles",
  "script": "// MP-Q2: ERPNext's global Employee link formatter labels every Employee link on a form with the form's\n// own employee_name, so \"Reports To\" showed the employee's name instead of the manager's. Stricter version.\nfrappe.form.link_formatters['Employee'] = function (value, doc, df) {\n    if (!doc || !value) return value;\n    if (doc.employee === value && doc.employee_name && doc.employee_name !== value) return value + ': ' + doc.employee_name;\n    if (df && df.fieldname === 'reports_to' && doc.line_manager_name) return value + ': ' + doc.line_manager_name;\n    return value;\n};\n",
  "view": "Form"
 }
]